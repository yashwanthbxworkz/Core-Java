class DoorDash{
	static double ref = 0.0;
	public static double search(String foodName){
		if(foodName == "bread")return ref = 80.0;
	
	return ref=0.0;

}
		public static double search(String foodName, int qty){
			
		
		if(foodName == "bread")return ref = 80.0*qty;
		
		
		return ref = 0.0;
}
}

